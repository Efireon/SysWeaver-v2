package structures

// База Jail конфига
type JailConfig struct {
	// Сюда нужно прописать структуру изоляции
	// Например pid namespaces, user namespaces, chroot dir, переменные окружения(список, так как это динамическая переменная), путь до билдера, и путь до шаблона

}
